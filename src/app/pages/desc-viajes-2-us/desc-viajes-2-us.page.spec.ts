import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DescViajes2UsPage } from './desc-viajes-2-us.page';

describe('DescViajes2UsPage', () => {
  let component: DescViajes2UsPage;
  let fixture: ComponentFixture<DescViajes2UsPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DescViajes2UsPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
